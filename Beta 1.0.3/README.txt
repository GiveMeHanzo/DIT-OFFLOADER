================================================================================
  DIT OFFLOADER  v1.0.3  —  数字影像工程素材备份与校验工具
================================================================================

  适用场景：影视片场的 DIT（数字影像工程）工作流程 ——
  将摄影机存储卡中的原始媒体素材（MXF, MOV, R3D, BRAW 等）
  安全备份到目标硬盘，并对每份文件进行 XXHash-64 校验，
  生成可视化 HTML 场记报告。


【主要功能】

  ● 素材拷贝 —— 并行多线程，支持断点续传和多种重名策略
  ● XXHash-64 校验 —— C 扩展实现，比 MD5 快数倍
  ● 场记重命名 —— 导入 SSCL 场记板 XML，自动按场/镜/次/机位
    重命名媒体文件及附属 XML
  ● HTML 报告 —— 视频缩略图 + 元数据 + 校验值 + 文件清单
  ● 深色界面 —— 三栏布局，支持拖拽添加源路径


【系统要求】

  Windows 10/11 (64-bit)
  macOS 13+ (Intel / Apple Silicon)

  无需预装 Python 或任何依赖。


【使用方法】

  1. 启动 DIT OFFLOADER
  2. 左侧面板浏览驱动器，拖拽或双击添加源文件夹
  3. 中间面板设置目标路径、作业名称、重名策略
  4. 可选：导入 SSCL 场记板 XML 文件启用自动重命名
  5. 勾选"生成 HTML 报告"（默认开启）
  6. 点击 "Start Copy" 开始备份


【输出文件】

  作业完成后，目标目录下会生成以下文件：

    {作业名}_log.xml       — XML 日志（断点续传依据）
    {作业名}_report.html   — HTML DIT 报告（浏览器打开）


┌──────────────────────────────────────────────────────────────────────────────
│  Windows 安装与启动
└──────────────────────────────────────────────────────────────────────────────

  直接双击 DIT Offload.exe 即可运行，无需安装。

  如弹出 "Windows 已保护你的电脑" 提示，点击「更多信息」→「仍要运行」。


┌──────────────────────────────────────────────────────────────────────────────
│  macOS 安装与启动
└──────────────────────────────────────────────────────────────────────────────

  ■ 从 DMG 安装（推荐）

    1. 双击 DIT Offload.dmg 挂载磁盘映像
    2. 将 DIT OFFLOADER.app 拖入 Applications 文件夹
    3. 弹出 DMG（桌面右键 → 推出）
    4. 从启动台或 Applications 文件夹双击运行

  ■ 首次启动提示"无法验证开发者"

    由于未经过 Apple 公证，首次启动时 macOS 会阻止运行。
    按以下步骤绕过（只需操作一次）：

    │ 方法 A（推荐）：
    │   右键（或 Ctrl+点击）DIT OFFLOADER.app →「打开」
    │   在弹出的对话框点击「打开」
    │
    │ 方法 B：
    │   系统设置 → 隐私与安全性 → 向下滚动到"安全性"
    │   → 找到"已阻止 DIT OFFLOADER.app" → 点击「仍要打开」

    之后再次启动无需重复此操作。


┌──────────────────────────────────────────────────────────────────────────────
│  FFmpeg 安装（强烈建议安装）
└──────────────────────────────────────────────────────────────────────────────

  FFmpeg 用于报告中的视频缩略图提取和元数据显示。
  未安装时程序仍可正常完成拷贝和校验，仅报告中视频预览不可用。

  ── Windows ──

  安装方式（任选一种）：

    1. winget（推荐，一键安装）：
       winget install Gyan.FFmpeg

    2. Scoop：
       scoop install ffmpeg

    3. Chocolatey：
       choco install ffmpeg

    4. 手动安装（离线 / 无管理员权限）：
       a) 从 https://www.gyan.dev/ffmpeg/builds/ 下载
          ffmpeg-release-essentials.zip
       b) 解压后将 bin 文件夹复制到 C:\ffmpeg\bin\
       c) 确保 C:\ffmpeg\bin\ 下存在 ffmpeg.exe 和 ffprobe.exe

  安装后程序会自动检测，无需配置环境变量。

  ── macOS ──

  ■ 安装 Homebrew（如尚未安装）：
    打开终端（启动台 → 其他 → 终端），粘贴以下命令并回车：

    /bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"

    安装过程中按提示输入密码并按回车确认。

  ■ 安装 FFmpeg：
    终端中运行：

    brew install ffmpeg

    安装完成后终端输入 ffmpeg -version 看到版本信息即成功。

  DIT OFFLOADER 启动时自动检测 /opt/homebrew/bin/ 和 /usr/local/bin/，
  无需额外配置。


┌──────────────────────────────────────────────────────────────────────────────
│  技术信息
└──────────────────────────────────────────────────────────────────────────────

  版本：Beta 1.0.3
  日期：2026-06-24


┌──────────────────────────────────────────────────────────────────────────────
│  开源协议
└──────────────────────────────────────────────────────────────────────────────

  本软件使用了以下开源组件，依照其各自许可协议分发：

  PySide6             LGPL v3       https://pypi.org/project/PySide6/
  xxhash              BSD 2-Clause  https://pypi.org/project/xxhash/
  psutil              BSD 3-Clause  https://pypi.org/project/psutil/
  FFmpeg              LGPL v2.1+    https://ffmpeg.org/

  依据 LGPL 要求，您有权获取上述 LGPL 组件的完整源代码。
  如需获取，请联系软件分发方或访问对应项目官网链接。

  FFmpeg is a trademark of the FFmpeg project.
================================================================================

# DIT Offload 软件依赖
# 安装：python -m pip install -r requirements.txt
# 最低 Python 版本：3.10

# GUI 框架（LGPL，跨平台原生风格；PySide6>=6.6 支持 Qt 6.6）
PySide6>=6.6

# xxHash-64 极速校验（C 扩展，远快于 hashlib）
xxhash>=3.4

# 跨平台磁盘/驱动器枚举（drive_panel.py）
psutil>=5.9

# ── 以下为外部工具依赖（非 Python 包）────────────────────────────
# FFmpeg / FFprobe（报告元数据提取 + 视频抽帧）
# - 下载：https://ffmpeg.org/download.html
# - Windows：winget install Gyan.FFmpeg
# - macOS：brew install ffmpeg
# - 缺失不阻断主流程（拷贝/校验正常运行），仅报告中的视频缩略图
#   和元数据提取功能不可用（main.py 启动时会打印警告）。
